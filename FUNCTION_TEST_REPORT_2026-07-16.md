# DocAssist 실제 기능 테스트 보고서

- 실행일: 2026-07-16
- 대상: `http://localhost:3000` / 실제 로컬 백엔드와 PostgreSQL
- 방식: Playwright CLI, Chromium headless, API 모킹 없음
- 화면: 데스크톱 1440x1000, 모바일 390x844, 라이트/다크
- 테스트 전 기준: 사용자 2명, 문서 6건, 채팅 세션 7건, 용어 36건

## 수정 결과 (2026-07-16)

- Q&A: 분석 중 문서 선택을 비활성화하고 세션 생성·질문 API에서도 HTTP `409`로 차단
- Q&A: 문서 근거가 없으면 LLM을 호출하지 않고 문서에 없다고 답하며 관련 없는 근거를 제거
- Q&A: `제문단 4조` 형태의 근거 제목을 `4번 문단` 형태로 수정하고 대표 제목을 `h1`으로 변경
- 업로드: 0바이트와 텍스트 추출 불가 파일을 DB 저장 전에 HTTP `400`으로 차단
- 드라이브: 상태 필터와 문서 삭제 버튼을 실제 삭제 API에 연결
- 프로필: 비밀번호 변경 진입 버튼, 한국어 비밀번호 오류, 중복 로그아웃 제거
- 용어집: 문서·분류·저장한 용어 필터를 화면에 연결
- 문의: 생성·관리자 목록·답변을 `support_tickets` DB에 연결하고 사용자가 프로필의 문의 내역에서 답변을 확인하도록 구현
- 관리자: 최근 8일 가입·변환 추이를 실제 DB에서 집계하고 시스템 상태를 API 상태와 연결
- 관리자: 사용자·문서·문의 빈 상태와 라이트 모드 글자 대비 개선
- 피드백: 쉬운말 결과 평가를 만족도 API에 저장하고 다시 불러오도록 연결
- 국어사전: 일시적 네트워크·5xx 오류 1회 재시도 추가, 실제 `계약` 검색 성공 확인

검증: Q&A 단위 테스트 `2/2 PASS`, API 통합 테스트 PASS, 프론트 빌드 PASS, Playwright `5/5 PASS`.

## 판정 기준

- PASS: 핵심 흐름과 결과가 정상임
- PARTIAL: 핵심 흐름은 동작하지만 일부 기능 또는 UX가 빠져 있음
- FAIL: 잘못된 결과가 나오거나 사용 흐름이 중단됨
- EXTERNAL: 외부 API 권한·키·서버 문제로 앱과 분리되는 실패

## 테스트 결과

### 2. 텍스트 쉬운말 변환 - PARTIAL

- UI 검증 `22/22 PASS`, 변환 API 모두 HTTP `200`
- 빈 입력 차단, 5,000자 제한, 로딩, 결과 화면, 변경 표현 하이라이트와 선택 정상
- 쉬운말·나란히 비교·요약/용어 탭, 세 단계 재변환, 원문 전환, 복사, 모바일·다크모드 정상
- FAIL: 이모티콘 피드백은 화면만 바뀌고 서버에 저장되지 않으며 새로고침하면 사라짐
- WARN: 일반 문서 변환 약 33.5~43.9초, 최대 44초로 체감 대기 시간이 김
- WARN: 동일 입력·강도에서도 변경 표현 수와 결과 길이가 달라짐

### 3. 파일 업로드 - PARTIAL

- TXT·DOCX·PDF 업로드 HTTP `201`, 진행 상태와 문서 상세 이동 정상
- 비지원 CSV는 HTTP `400`과 올바른 안내 표시
- FAIL: 0바이트 TXT를 업로드 전에 차단하지 않고 접수한 뒤 뒤늦게 `분석 실패`가 표시됨
- WARN: 처리된 비지원 파일 검증 오류도 브라우저 콘솔에 Axios 오류로 기록됨

### 4. 실제 문서 보기 - PASS

- TXT 변경 표현 하이라이트 12개 정상
- DOCX 원본 구조 렌더링과 하이라이트 12개 정상
- PDF 캔버스 `862x1220`, 실제 페이지 픽셀 렌더링 확인
- PDF 좌표 하이라이트 2개와 오른쪽 표현 목록 선택 동기화 정상
- 원문 전환 애니메이션과 최종 원문 모드 정상
- TXT 원문·쉬운말, DOCX·PDF 쉬운말 다운로드 정상

### 5. 드라이브 - PARTIAL

- 목록, 검색, 유형 필터, 제목순 정렬, 문서 다시 열기 정상
- 다크모드 대비와 모바일 가로 넘침 정상
- FAIL: 완료·분석 중·실패를 거르는 상태 필터가 없음
- FAIL: 백엔드 삭제 API는 있으나 문서 삭제 UI가 없음

### 6. Q&A 챗봇 - FAIL

- 일반 질문 HTTP `200`, 분석 완료 문서 질문과 문서 밖 질문은 정상
- 완료 문서의 날짜 질문에 `2026년 7월 20일 오전 10시`로 답하고 근거 5개 표시
- 문서에 없는 출장비 질문은 없다고 정상 안내
- FAIL: 분석 중인 문서를 선택하고 질문할 수 있어 근거 없이 잘못된 날짜와 문서에 없는 규정을 답함
- FAIL: 일부 근거 제목이 `제문단 4조 · 1번 문단`처럼 잘못 조합됨
- WARN: 문서에 없다고 답하면서도 관련 없는 근거 5개가 표시되는 경우가 있음

핵심 재현: 파일 업로드 직후 Q&A에서 분석 중 문서를 선택하고 내용 질문 또는 문서 밖 질문을 전송한다.

### 7. 프로필과 화면 설정 - PARTIAL

- 이름 수정 HTTP `200`, 글자 크기 작게 `15px`·중간 `16.5px`·크게 `18.5px`·커스텀 `21.5px` 정상
- 다크모드와 글자 크기가 새로고침·재로그인 뒤에도 유지됨
- 정상 비밀번호 변경과 변경된 비밀번호 재로그인 성공
- FAIL: 프로필에 비밀번호 변경 화면으로 이동하는 버튼이나 링크가 없어 URL을 직접 입력해야 함
- FAIL: 잘못된 현재 비밀번호 오류가 `Incorrect password`로 영어 표시됨
- WARN: 사이드바와 프로필 본문에 로그아웃 버튼이 중복됨

### 8. 용어집과 국어사전 - PARTIAL / EXTERNAL

- 빈 상태, 문서 용어 표시, 검색, 고정·해제와 성공 토스트 정상
- FAIL: 문서 필터, 분류 필터, 저장한 용어만 보기 UI가 없음
- EXTERNAL: 국어사전 검색이 HTTP `502`; 화면 오류 안내는 정상 표시
- 성공 검색과 외부 사전 API 정상 상태는 현재 환경에서 검증하지 못함

### 9. 관리자 화면 - PARTIAL

- 일반 사용자 메뉴 미노출, `/admin` 접근 차단, 관리자 API 직접 호출 `403` 정상
- 관리자 API·사용자·문서 API `200`, 검색과 실제 DB 통계 스냅샷 일치
- 확인 스냅샷: 회원 3, 당일 로그인 2, 접속자 1, 문서 5, 오늘 업로드 0, 챗봇 11, 용어 31
- FAIL: 프로필 문의 기능은 API·DB와 연결되지 않았고 관리자 문의 3건·대기 2건은 고정값
- FAIL: 가입·변환 기간별 추이 그래프가 실제 통계가 아닌 고정 SVG 좌표
- FAIL: 사용자·문서 검색 결과가 0건일 때 빈 상태 안내가 없음
- FAIL: 상단 `상태: 정상` 문구가 API 상태와 무관한 고정값

### 10. 공통 품질 - PARTIAL

- Playwright 디자인 회귀 테스트 `5/5 PASS`
- 데스크톱·모바일·다크모드·텍스트 결과·PDF 좌표 하이라이트 재실행 성공
- 주요 7개 화면의 데스크톱/모바일·라이트/다크 총 28개 조합에서 가로 넘침·겹침·잘림 없음
- 빠른 탭 전환, 메뉴 반복 클릭, 잘못된 문서 ID 오류 화면 정상
- FAIL: 관리자 라이트 모드 일부 보조 글자 대비 `3.76:1`, `4.18:1`로 권장 `4.5:1` 미달
- 접근성: Q&A 대표 제목이 `h1/h2`가 아닌 `strong`; 키보드 포커스는 표시됨

## 우선 수정 권장 순서

1. 분석 중 문서를 Q&A 선택 목록에서 비활성화하고 서버에서도 질문을 거부
2. 관리자 문의·기간별 추이·상단 상태의 고정값 제거 및 실제 API 연결
3. 0바이트·추출 텍스트 없음 파일을 업로드 전에 차단
4. 드라이브 상태 필터와 문서 삭제 UI 추가
5. 비밀번호 변경 진입 버튼과 한국어 오류 문구 추가
6. 국어사전 API `502` 원인 확인 및 연결 복구
7. 피드백 저장, 용어집 필터, 관리자 빈 상태와 색 대비 개선

## 테스트 하네스 특이사항

- 최초 병렬 GUI Chrome 시작 중 macOS HIServices `SIGABRT`가 1회 발생함
- DocAssist 코드 문제가 아닌 테스트 Chrome 시작 충돌로 분류함
- Chromium headless 단일 워커로 재실행했으며 충돌 없이 `5/5 PASS`
- 최초 PDF 픽셀 검사는 렌더 완료 전에 실행되어 흰 화면으로 잡혔으나, 지연 재검사에서 정상 렌더링을 확인함

## 테스트 데이터 정리

- 모든 테스트 계정·문서·채팅·용어·임시 파일 정리 완료
- 최종 DB: 사용자 2명, 문서 6건, 채팅 세션 7건, 용어 36건
- 테스트 전 기준과 동일하며 기존 데이터는 삭제하지 않음
